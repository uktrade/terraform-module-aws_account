{
  "Statement": [{
    "Sid": "AWSConfigSNSPolicy",
    "Effect": "Allow",
    "Principal": {
      "AWS": "${config_sns_role}"
    },
    "Action": "SNS:Publish",
    "Resource": "${config_sns_arn}"
  }]
}
