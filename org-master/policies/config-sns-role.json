{
  "Version": "2012-10-17",
  "Statement": [{
    "Sid": "AWSConfigSNSPolicy",
    "Effect": "Allow",
    "Principal": {
      "AWS": "${config_sns_role}"
    },
    "Action": "SNS:Publish",
    "Resource": "${config_sns_arn}"
  }]
}
